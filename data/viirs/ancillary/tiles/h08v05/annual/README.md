# Annual Ancillary Products For h08v05

Use one subdirectory per water year or processing year, for example `2026/`.

Suggested products:

- `r0_reflectance.zarr` or `r0_reflectance.nc`
- `r0_support.zarr`
- `qa_summary.zarr`
- `snow_prior.zarr`
