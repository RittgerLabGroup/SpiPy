# Static Ancillary Products For h08v05

Products here should share the VIIRS 500 m tile grid and include `x`, `y`, and `spatial_ref`.

Suggested products:

- `canopy_fraction.zarr`
- `water_mask.zarr`
- `dem.zarr`
- `glacier_ice_fraction.zarr`
- `landcover.zarr`
